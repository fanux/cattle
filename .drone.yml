build:
   image: 192.168.86.106/devops/golang:1.7-godep
   commands:
     - mkdir -p $GOPATH/src/github.com/docker/swarm/ && cp -r ./* $GOPATH/src/github.com/docker/swarm/
     - cd $GOPATH/src/github.com/docker/swarm/ && godep go build -o cattle && cp cattle /drone/cattle/

publish:
   docker:
      username: admin
      password: Harbor12345
      registry: 192.168.86.106
      email: fhtjob@hotmail.com
      repo: devops/cattle
      tag: alpha-v1.0
      file: Dockerfile
      insecure: true
